<%= form_for(@inventory_status) do |f| %>
  <% if @inventory_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@inventory_status.errors.count, "error") %> prohibited this inventory_status from being saved:</h2>

      <ul>
      <% @inventory_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>
